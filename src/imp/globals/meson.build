# builds the globals 


inc = [
    inc_filesystem,
    inc_i_filesystem
]
g_filesystem_lib = library('g_filesystem', 'g_filesystem.cpp',
                                include_directories : inc,
                                link_with : filesystem_lib)
